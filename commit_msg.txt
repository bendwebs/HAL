fix: add interrupt call and delay before SD generation

Call the SD interrupt endpoint before each generation to clear any
pending jobs, then wait 0.5s for SD to settle. This may fix the
issue where sequential image generations hang.
