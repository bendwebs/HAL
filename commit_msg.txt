fix: SD availability check timeout and autolaunch flag

- Increased check_availability timeout from 5s to 10s (SD can be slow to respond)
- Changed log level to debug for successful availability checks
- Added --autolaunch=false flag to prevent browser from opening when SD starts
