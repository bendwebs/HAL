fix: disable HTTP keepalive for SD requests to prevent connection reuse issues

Force fresh connections for each SD API request by:
- Disabling HTTP/2
- Setting max_keepalive_connections=0
- Adding Connection: close header

This may fix the issue where second image generation hangs.
