fix: use COMMANDLINE_ARGS env var instead of passing args to webui.bat

The SD webui.bat script reads COMMANDLINE_ARGS environment variable
rather than command line arguments. Fixed the launcher to set this
variable before calling webui.bat.

Using --api --nowebui flags to enable API and run headless.
