fix: export API_URL from api.ts and use in generate page

The generate page was defining its own API_URL which could have a
different value than the centralized one. Now imports from api.ts
to ensure consistent URL usage.
